{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least-privilege capability for the single bundled main WebView.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
